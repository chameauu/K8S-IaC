---
# handlers file for nfs-server

- name: reload nfs exports
  command: exportfs -ra
  become: yes

- name: restart nfs server
  systemd:
    name: nfs-kernel-server
    state: restarted
  become: yes
